server {
    listen 80;
    server_name _;

    # X-ray and chart uploads can exceed nginx's 1m default.
    client_max_body_size 50m;

    location / {
        proxy_pass http://web:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# HTTPS: get certs on the host (certbot certonly --standalone, with the stack
# down), uncomment the letsencrypt mount in docker-compose.standalone.yaml, and
# replace the server block above with these two.
#
# server {
#     listen 80;
#     server_name mom.example.com;
#     return 301 https://$host$request_uri;
# }
#
# server {
#     listen 443 ssl;
#     server_name mom.example.com;
#
#     ssl_certificate     /etc/letsencrypt/live/mom.example.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/mom.example.com/privkey.pem;
#
#     client_max_body_size 50m;
#
#     location / {
#         proxy_pass http://web:3000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
